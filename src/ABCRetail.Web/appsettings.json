{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Functions": {
    "_comment": "Set Functions__BaseUrl and Functions__Key as application settings in Azure. The key is never stored here.",
    "BaseUrl": "",
    "Key": ""
  },
  "AzureStorage": {
    "BlobContainer": "product-images",
    "FileShare": "application-logs"
  },
  "ConnectionStrings": {
    "_comment": "Never store the real key here. Locally: dotnet user-secrets set \"ConnectionStrings:AzureStorage\" \"<value>\". In Azure: the ConnectionStrings__AzureStorage application setting.",
    "AzureStorage": ""
  }
}
